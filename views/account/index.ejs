<!-- Content Heading -->
<% if (title) { %>
    <h1><%= title %></h1>
  <% } else { %>
    <% res.redirect('/') %>
  <% } %>
  
    <!-- Message Block -->
    <%- messages() %>

    <% if (locals.accountType != 'Client') { %>
      <h2>Welcome Back <%= locals.clientName %></a>
        <h3>Account Management</h3>
        <a href="account/manage" class="manageText">Manage Account</a>
        <h3>Inventory Management</h3>
        <a href="inv/" class="manageText">Manage Inventory</a>
  <% } else { %>
     <h2>Welcome <%= locals.clientName %></a>
      <h3>Account Management</h3>
      <a href="account/manage" class="manageText">Manage Account</a>
      
  <% } %>

      <% if (locals.accountType === 'Admin') { %>
        <h3>Employee Management</h3>
        <a href="account/employees" class="manageText">Manage Employees</a>
        <% } %>